import { Text, StyleSheet } from "react-native";

const GameScreen = () => {
  return <Text style={styles.test}>GameScreen</Text>;
};

export default GameScreen;

const styles = StyleSheet.create({
  test: {
    color: "red",
  },
});
